<!-- __FILL_FROM_USER__:tracking_log
     FOR THE AGENT READING THIS — this is an APPEND-ONLY log, starts empty.
     What this doc is: the running history of maintenance runs and significant session
     changes. `/weekly-maintenance` and `/wind-down` append entries here; nothing is ever
     edited or deleted (append-only is the integrity guarantee). Read it only when tracing
     what a past run did. It is NOT tier-loaded.
     Your job: just keep appending dated entries. No fill-in needed beyond that. -->

# TRACKING LOG
*Append-only record of maintenance runs + significant session changes. Never edit prior entries.*

| Date | Actor | What changed | Why |
|------|-------|--------------|-----|
| _(YYYY-MM-DD)_ | _(skill / session)_ | _(summary + counts)_ | _(rationale / carry-over lineage)_ |

<!-- new entries append below this line -->
